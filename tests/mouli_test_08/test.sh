#!/bin/bash

echo '################ 2*push, sub(2*positive), dump and exit (EXPECTED) ###########'
echo '-789'
echo '################ 2*push, sub(2*positive), dump and exit (GOT) ################'
../.././abstractVM 2>/dev/null < input